# Appendix

## Data format specifications

### Transcriptomics upload format

[Expression matrix format: rows/columns, ID type (gene symbol/Ensembl), required sample metadata columns (group, sex, etc.), accepted file types (CSV, RData).]

### Methylomics upload format

[Accepted inputs: raw IDAT pairs vs. a pre-computed beta-value matrix, required sample sheet columns, array type supported (e.g. EPIC/450K).]

## Glossary

[Define any domain-specific terms/abbreviations used throughout the guide that a non-specialist reader might not know: DMP, DMR, WGCNA, colocalization, Mendelian randomization, etc.]

## Scalability notes

[Optional: any practical limits on dataset size, expected run time for the slower modules (e.g. WGCNA, feature selection), and any recommendations for large cohorts.]

## Feature comparison

[Optional: if useful, a table comparing what's available in the Transcriptomics track vs. the Methylomics track, since the two tracks don't yet have identical module sets.]
