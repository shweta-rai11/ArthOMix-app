# 02_Overview

`TX_MODULES` stage `id = "overview"`, title "Overview and Datasets".

- **File**: `mod_overview.R`.
- **Input**: the shared `dataset` reactiveValues from `../01_Data/`.
- **Main analysis**: none — a QC dashboard for the currently loaded dataset plus a cross-module "sub-modules run so far" status roll-up.
- **Output**: no `results$*` write — display only.
- **UI**: Transcriptomics → Sub-modules → Overview and Datasets.
